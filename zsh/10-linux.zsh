# Linux-only overlay (clusters, workstations). Sourced only when
# `uname` is Linux. Keep this as bare as 10-mac.zsh -- module loads,
# cluster-specific paths, etc. belong here or in local.sh, not in the
# portable core.
