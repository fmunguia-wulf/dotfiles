# Omega-specific config. Sourced by both .zshrc and .bashrc, guarded by
# hostname containing "omega" (matches omega14, omega-a, omega-b, somega).

alias cscratch='cd /cscratch/$USER'
alias duh='du -sh *'
alias dfh='df -h'
